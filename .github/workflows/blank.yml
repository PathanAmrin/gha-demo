name : Hello world
on: 
  push:
    branches:
      -main
      -develop
      -release/**
  pull_request:
    branchges:
      -main
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "A demo build"


          

